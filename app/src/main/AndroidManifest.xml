<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InBihar">
        <activity
            android:name=".UI.MyOrdersActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity android:name=".UI.MeowBottomActivity" />
        <activity
            android:name=".UI.ChangePasswordActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity
            android:name=".UI.EditProfileActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity
            android:name=".UI.ItemDetailActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity
            android:name=".UI.ItemActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity
            android:name=".UI.BottomActivity"
            android:theme="@style/AppTheme.InBihar" />
        <activity android:name=".UI.Authentication.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.Authentication.SignInActivity" />
    </application>

</manifest>